{
  "type": "object",
  "properties": {
    "birthday": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of user's birthdate"
    },
    "created_time": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of user account creation"
    },
    "display_name": {
      "type": "string",
      "description": "The display name of the user"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "The email address of the user"
    },
    "photo_url": {
      "type": "string",
      "format": "uri",
      "description": "URL to the user's profile photo"
    },
    "uid": {
      "type": "string",
      "description": "Unique identifier for the user"
    },
    "instagram_handle": {
      "type": "string",
      "description": "The user's Instagram handle (optional field)"
    }
  },
  "required": ["birthday", "created_time", "display_name", "email", "uid"]
}